steering_type: "no_steering"  # baseline only — no steering vector applied
glp: false

alphas: []                    # unused for no_steering

batch_size: 8
guard_batch_size: 32
max_new_tokens: 128

guard_device: "cuda:0"

out_dir: "results/steering_baseline"
